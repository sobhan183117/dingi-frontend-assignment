/*
 * AppRoute Messages
 *
 * This contains all the text for the AppRoute container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppRoute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppRoute container!',
  },
});

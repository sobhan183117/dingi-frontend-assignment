/*
 * ItemList Messages
 *
 * This contains all the text for the ItemList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ItemList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ItemList container!',
  },
});

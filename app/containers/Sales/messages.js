/*
 * Sales Messages
 *
 * This contains all the text for the Sales container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Sales';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Sales container!',
  },
});
